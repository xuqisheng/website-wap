#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../fis3/bin/fis.js" "$@"
  ret=$?
else 
  node  "$basedir/../fis3/bin/fis.js" "$@"
  ret=$?
fi
exit $ret
